// Placeholder: the SomnioIntegrationTests target is scaffolding only.
// Repository and server-flow tests land later, alongside a `PostgresContainer`
// helper that auto-spawns Postgres on demand so the suite needs no env-var setup.
